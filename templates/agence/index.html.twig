{% extends 'base_user.html.twig' %}

{% block title %}Agences
{% endblock %}

{% block navigation%}
	<!-- Navigation-->
	{% include 'tools/navigation.html.twig'  %}
	<!-- Header-->
	{% include 'tools/header.html.twig'  %}

	<!-- Section-->
{% endblock %}


{% block body %}
	<div class="container">

		<h5>Agence index | </h>
		<hr style="width:100%; height:5px; background-color:#1B4F72 ">


		<table class="table">
			<thead>
				<tr>
					<th>Id</th>
					<th>NumeroAgence</th>
					<th>Responsable</th>
					<th>Employés</th>
					<th>Adresse</th>
					<th>actions</th>
				</tr>
			</thead>
			<tbody>
				{% for agence in agences %}
					<tr>
						<td>{{ agence.id }}
							|</td>
						<td>{{ agence.numeroAgence }}
							|
							{{ agence.adresse }}</td>
						<td>{{ agence.directeur.nom }}</td>
						<td></td>
						<td>{{ agence.adresse }}</td>
						<td>
							<a href="{{ path('agence_affichage', {'id': agence.id}) }}" class="btn btn-secondary">lire</a>

							<a href="{{ path('agence_edition', {'id': agence.id}) }}" class="btn btn-secondary">editer</a>

						</td>
					</tr>
				{% else %}
					<tr>
						<td colspan="4">Il n'y a pas d'agence pour le Moment</td>
					</tr>
				{% endfor %}
			</tbody>
		</table>

		<a href="{{ path('agence_nouveau') }}" class="btn btn-primary">Nouvelle Agence</a>

	</div>
{% endblock %}
