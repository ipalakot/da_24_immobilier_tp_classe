<form method="post" action="{{ path('app_categorie_clt_delete', {'id': categorie_clt.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categorie_clt.id) }}">
    <button class="btn">Delete</button>
</form>
