{% extends 'base.html.twig' %}

{% block title %}Directeur{% endblock %}

{% block body %}
    <h1>Directeur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ directeur.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ directeur.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ directeur.prenom }}</td>
            </tr>
            <tr>
                <th>Revenus</th>
                <td>{{ directeur.revenus }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('directeur_index') }}">Liste de</a>

    <a href="{{ path('directeur_edition', {'id': directeur.id}) }}">editer</a>

    {{ include('directeur/_delete_form.html.twig') }}
{% endblock %}
