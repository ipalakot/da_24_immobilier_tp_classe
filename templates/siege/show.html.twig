{% extends 'base.html.twig' %}

{% block title %}Siege{% endblock %}



{% block navigation%}
<!-- Navigation-->
	{% include 'tools/navigation.html.twig'  %}
	<!-- Header-->
	{% include 'tools/header.html.twig'  %}

	<!-- Section-->
{% endblock %}


{% block body %}
    <h1>Siege</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ siege.id }}</td>
            </tr>
            <tr>
                <th>Capital</th>
                <td>{{ siege.capital }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ siege.nom }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ siege.adresse }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('siege_index') }}">Liste de</a>

    <a href="{{ path('siege_edition', {'id': siege.id}) }}">Modifier</a>

    {{ include('siege/_delete_form.html.twig') }}
{% endblock %}
