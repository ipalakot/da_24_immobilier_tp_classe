{% extends 'base.html.twig' %}

{% block title %}Siege{% endblock %}

{% block body %}
    <h1>Siege</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ siege.id }}</td>
            </tr>
            <tr>
                <th>Capital</th>
                <td>{{ siege.capital }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ siege.nom }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ siege.adresse }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_siege_index') }}">back to list</a>

    <a href="{{ path('app_siege_edit', {'id': siege.id}) }}">edit</a>

    {{ include('siege/_delete_form.html.twig') }}
{% endblock %}
