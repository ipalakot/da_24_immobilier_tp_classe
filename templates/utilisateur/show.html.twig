{% extends 'base_admin.html.twig' %}


{% block title %}Utilisateur
{% endblock %}
{% block body %}

	<!-- breadcrumb-->
	<nav aria-label="breadcrumb">
			<ol class="breadcrumb">
				<li class="breadcrumb-item">
					<a href="{{path('admin_index')}}">Administrator </a>

				</li>
							</li>
				<li class="breadcrumb-item">
					<a href="{{path('utilisateur_index')}}">Utilisteurs</a>

				</li>

				<li class="breadcrumb-item active" aria-current="page">{{ app.user.userIdentifier }}  </li>

				
			</ol>
				<hr style="width:100%; height:5px; background-color:#1B4F72 ">
		</nav>	
	<!-- End breadcrumb-->

	 
	<div class="container">
		<hr/>
		<h1>{{ utilisateur.id }}
			/
			{{ utilisateur.nom }}
			{{ utilisateur.prenoms }}</h1>
		<hr/>

		<table class="table">
			<tbody>
				<tr>
					<th>Id</th>
					<td>{{ utilisateur.id }}</td>
				</tr>
				<tr>
					<th>Nom</th>
					<td>{{ utilisateur.nom }}</td>
				</tr>
				<tr>
					<th>Prenoms</th>
					<td>{{ utilisateur.prenoms }}</td>
				</tr>
				<tr>
					<th>Adresse</th>
					<td>{{ utilisateur.adresse }}</td>
				</tr>
				<tr>
					<th>DateNaissance</th>
					<td>{{ utilisateur.age }}</td>
				</tr>
				<tr>
					<th>Telephone</th>
					<td>{{ utilisateur.phone }}</td>
				</tr>
				<tr>
					<th>Email</th>
					<td>{{ utilisateur.Email }}</td>
				</tr>
				<tr>
					<th>Login</th>
					<td>{{ utilisateur.login }}</td>
				</tr>
				<tr>
					<th>Password</th>
					<td>{{ utilisateur.password }}</td>
				</tr>
			</tbody>
		</table>

		<a href="{{ path('utilisateur_index') }}" class="btn btn-primary">Les utilisateurs</a>

		<a href="{{ path('utilisateur_edition', {'id': utilisateur.id}) }} " class="btn btn-primary">Mettre à jour</a>
		<br>

		{{ include('utilisateur/_delete_form.html.twig') }}

	</div>
{% endblock %}
