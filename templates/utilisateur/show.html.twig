{% extends 'base.html.twig' %}

{% block title %}Utilisateur{% endblock %}


{% block navigation%}
<!-- Navigation-->
	{% include 'tools/navigation.html.twig'  %}
	<!-- Header-->
	{% include 'tools/header.html.twig'  %}

	<!-- Section-->
{% endblock %}


{% block body %}
	<div class="container">
		<hr/>
		<h1>{{ utilisateur.id }} / {{ utilisateur.nom }}  {{ utilisateur.prenoms }}</h1>
        <hr/>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ utilisateur.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ utilisateur.nom }}</td>
            </tr>
            <tr>
                <th>Prenoms</th>
                <td>{{ utilisateur.prenoms }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ utilisateur.adresse }}</td>
            </tr>
            <tr>
                <th>DateNaissance</th>
                <td>{{ utilisateur.age }}</td>
            </tr>
            <tr>
                <th>Telephone</th>
                <td>{{ utilisateur.phone }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ utilisateur.Email }}</td>
            </tr>
            <tr>
                <th>Login</th>
                <td>{{ utilisateur.login }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ utilisateur.password }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('utilisateur_index') }}" class="btn btn-primary">Les utilisateurs</a>

    <a href="{{ path('utilisateur_edition', {'id': utilisateur.id}) }} " class="btn btn-primary">Mettre à jour</a> <br>

    {{ include('utilisateur/_delete_form.html.twig') }}

    </div>
{% endblock %}
